o	○ git init (creates (local and possibly remote) Master branch)
		git add README.txt
		git commit –m “adding README.txt“
		gaproject





		it branch
		a* master
		Git branch -M main (sets branch name to main)
		git remote add origin http://<your git server>/<your repo>.git || log into github via browser and add a repository there.
git push -u origin master (causes a login, check for the window)
